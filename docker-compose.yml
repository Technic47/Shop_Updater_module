version: '3.8'

services:
  updater_module:
    env_file:
      - "../.env"
    build:
      context: .
    container_name: updater_module
    restart: always
    environment:
      MODULE_GROUP_ID: UPDATER_GROUP_ID
#    deploy:
#      mode: replicated
#      replicas: 2

